import {
    create,
    NSelect,
    NButton,
    NTag,
    NDataTable,
    NLayout,
    NLayoutHeader,
    NLayoutContent,
    NMessageProvider,
    NCard,
  } from 'naive-ui'
  
  export const naive = create({
    components: [
      NSelect,
      NButton,
      NTag,
      NDataTable,
      NLayout,
      NLayoutHeader,
      NLayoutContent,
      NMessageProvider,
      NCard,
    ],
  })
  